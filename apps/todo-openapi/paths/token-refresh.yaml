patch:
  summary: Refresh JWT using refresh token cookie
  tags: [users]
  responses:
    "200":
      description: New JWT issued
      content:
        application/json:
          schema:
          type: object
          properties:
            token:
            type: string
